import 'package:flutter/material.dart';

class anysuggestion extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Center(
      child: new Text("Any Suggestion",
        style: TextStyle(
            fontSize: 25.0),
      ),

    );
  }

}