import 'package:flutter/material.dart';

class Term extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Center(
      child: new Text("Term & Condition",
        style: TextStyle(
            fontSize: 25.0),
      ),

    );
  }

}